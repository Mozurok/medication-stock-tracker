import { expect, test } from 'vitest'

// Placeholder so the CI `test` step is a real run rather than passWithNoTests.
// Slice 2 replaces this with the domain tests.
test('the test runner is wired up', () => {
  expect(true).toBe(true)
})
